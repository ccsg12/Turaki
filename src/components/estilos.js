import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    titulo: {
        fontSize: 40,
        fontWeight: 'bold',
        color: '#fff',
    },
    subtitulo: {
        fontSize: 35,
        fontWeight: 'bold',
    },
    subtitulo1: {
        fontSize: 16,
        fontWeight: 'bold',
        color: '#fff',
    }
})