import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    titulo: {
        fontSize: 40,
        fontWeight: 'bold',
    },
    subtitulo: {
        fontSize: 35,
        fontWeight: 'bold',
    },
})