import React from "react";
import Icon from "react-native-vector-icons/Fontisto";

export default (props) => {
  const sexIcon = <Icon name="intersex" size={20} color={props.color} />;

  return sexIcon;
};
